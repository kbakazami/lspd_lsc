---
const navLinks = [
  { label: 'Accueil', href: '/' },
  { label: 'Actualités', href: '/actualites' },
  { label: 'Organigramme', href: '/organigramme' },
  { label: 'Divisions', href: '/divisions' },
  { label: 'Recrutement', href: '/recrutement' },
];
const currentPath = Astro.url.pathname;
const isActive = (href: string) =>
  href === '/' ? currentPath === '/' : currentPath === href || currentPath.startsWith(href + '/');
---

<header class="bg-lspd-navy border-b border-lspd-gold/20 sticky top-0 z-50">
  <div class="max-w-7xl mx-auto px-6 lg:px-8">
    <div class="flex items-center justify-between h-[72px]">

      <!-- Identité -->
      <a href="/" class="flex items-center gap-3.5 group">
        <img
          src="/lspd-seal.webp"
          alt="LSPD Seal"
          width="44"
          height="44"
          class="w-11 h-11 shrink-0 object-contain group-hover:scale-105 transition-transform duration-200"
        />
        <div>
          <span class="block text-[10px] text-lspd-gold font-mono tracking-[0.28em] uppercase leading-none opacity-80">
            Los Santos
          </span>
          <span class="block text-white font-display text-xl font-semibold tracking-wide leading-tight">
            Police Department
          </span>
        </div>
      </a>

      <!-- Navigation desktop -->
      <nav class="hidden lg:flex items-center">
        {navLinks.map(link => (
          <a
            href={link.href}
            class={`relative px-4 py-2 text-sm tracking-wide transition-colors duration-150 ${
              isActive(link.href)
                ? 'text-lspd-gold'
                : 'text-white/55 hover:text-white/90'
            }`}
          >
            {link.label}
            {isActive(link.href) && (
              <span class="absolute bottom-0 left-4 right-4 h-px bg-lspd-gold"></span>
            )}
          </a>
        ))}
      </nav>

      <!-- CTA desktop -->
      <a
        href="/recrutement"
        class="hidden lg:inline-flex items-center px-5 py-2.5 border border-lspd-gold/60 text-lspd-gold text-xs font-medium tracking-[0.2em] uppercase hover:bg-lspd-gold hover:text-lspd-navy transition-all duration-200"
        style="border-radius: var(--radius-sm)"
      >
        Candidater
      </a>

      <!-- Bouton burger mobile -->
      <button
        id="burger-btn"
        class="lg:hidden text-white/70 hover:text-white p-2 transition-colors"
        aria-label="Ouvrir le menu"
        aria-expanded="false"
        aria-controls="mobile-menu"
      >
        <!-- Icône burger -->
        <svg id="icon-burger" width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round">
          <path d="M3 6h18M3 12h18M3 18h18"/>
        </svg>
        <!-- Icône fermeture -->
        <svg id="icon-close" width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" class="hidden">
          <path d="M18 6L6 18M6 6l12 12"/>
        </svg>
      </button>
    </div>
  </div>

  <!-- Menu mobile (panneau déroulant) -->
  <div
    id="mobile-menu"
    class="lg:hidden hidden border-t border-lspd-gold/20 bg-lspd-navy-deep"
    aria-hidden="true"
  >
    <nav class="max-w-7xl mx-auto px-6 py-4 flex flex-col">
      {navLinks.map(link => (
        <a
          href={link.href}
          class={`flex items-center justify-between py-3.5 border-b border-white/8 text-sm tracking-wide transition-colors duration-150 ${
            isActive(link.href)
              ? 'text-lspd-gold'
              : 'text-white/65 hover:text-white'
          }`}
        >
          {link.label}
          {isActive(link.href) && (
            <span class="w-1.5 h-1.5 rounded-full bg-lspd-gold"></span>
          )}
        </a>
      ))}

      <a
        href="/recrutement"
        class="mt-5 flex items-center justify-center px-5 py-3 border border-lspd-gold/60 text-lspd-gold text-xs font-medium tracking-[0.2em] uppercase hover:bg-lspd-gold hover:text-lspd-navy transition-all duration-200"
        style="border-radius: var(--radius-sm)"
      >
        Candidater
      </a>
    </nav>
  </div>
</header>

<script>
  const btn = document.getElementById('burger-btn') as HTMLButtonElement;
  const menu = document.getElementById('mobile-menu') as HTMLElement;
  const iconBurger = document.getElementById('icon-burger') as SVGElement;
  const iconClose = document.getElementById('icon-close') as SVGElement;

  function openMenu() {
    menu.classList.remove('hidden');
    menu.setAttribute('aria-hidden', 'false');
    btn.setAttribute('aria-expanded', 'true');
    btn.setAttribute('aria-label', 'Fermer le menu');
    iconBurger.classList.add('hidden');
    iconClose.classList.remove('hidden');
    document.body.style.overflow = 'hidden';
  }

  function closeMenu() {
    menu.classList.add('hidden');
    menu.setAttribute('aria-hidden', 'true');
    btn.setAttribute('aria-expanded', 'false');
    btn.setAttribute('aria-label', 'Ouvrir le menu');
    iconBurger.classList.remove('hidden');
    iconClose.classList.add('hidden');
    document.body.style.overflow = '';
  }

  btn.addEventListener('click', () => {
    const isOpen = btn.getAttribute('aria-expanded') === 'true';
    isOpen ? closeMenu() : openMenu();
  });

  // Fermer sur clic d'un lien
  menu.querySelectorAll('a').forEach(link => {
    link.addEventListener('click', closeMenu);
  });

  // Fermer sur resize vers desktop
  const mql = window.matchMedia('(min-width: 1024px)');
  mql.addEventListener('change', (e) => {
    if (e.matches) closeMenu();
  });
</script>
